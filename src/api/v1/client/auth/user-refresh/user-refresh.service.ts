import { ClientUserRefreshResponseDto } from "@/api/v1/client/auth/user-refresh/dto/user-refresh.dto";

// 외주업체 사용자 토큰 재발급 서비스 자리입니다.
export const refreshClientUserToken = async (): Promise<ClientUserRefreshResponseDto> => {
  throw new Error("refreshClientUserToken is not implemented yet");
};
